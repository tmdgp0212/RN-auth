import React from "react";
import AuthContent from "../components/Auth/AuthContent";

const LoginScreen = () => {
  // onAuthenticate 매개변수 추가 필요
  return <AuthContent isLogin />;
};

export default LoginScreen;
